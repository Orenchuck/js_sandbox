{
  "name": "lesson_6",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "email-validator": "^2.0.4",
    "format-validator": "^0.9.0",
    "random-email": "^1.0.3"
  },
  "devDependencies": {
    "cssnano": "^4.1.10",
    "gh-pages": "^2.0.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel ./index.html",
    "build": "rm -rf dist && parcel build ./index.html --public-url /js_sandbox",
    "deploy": "gh-pages -d dist"
  },
  "author": "",
  "license": "ISC"
}
